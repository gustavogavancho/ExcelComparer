<Window x:Class="ExcelComparer.WPFUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:ExcelComparer.WPFUI.Models"
        mc:Ignorable="d"
        Title="Excel Diff (MVP)" Height="720" Width="1200" WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top controls -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Archivo A" Grid.Column="0" Padding="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox VerticalAlignment="Center" Text="{Binding FileA, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2" Content="..." Width="34" Command="{Binding BrowseFileACommand}"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="Archivo B" Grid.Column="2" Padding="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox VerticalAlignment="Center" Text="{Binding FileB, UpdateSourceTrigger=PropertyChanged}" />
                    <Button Grid.Column="2" Content="..." Width="34" Command="{Binding BrowseFileBCommand}"/>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Main -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0">
                <StackPanel DockPanel.Dock="Top" Orientation="Vertical" Margin="0,0,0,8">
                    <WrapPanel>
                        <CheckBox Content="Valores" IsChecked="{Binding CompareValues}" Margin="0,0,10,0"/>
                        <CheckBox Content="Fórmulas" IsChecked="{Binding CompareFormulas}" Margin="0,0,10,0"/>
                        <CheckBox Content="Incluir hojas ocultas" IsChecked="{Binding IncludeHiddenSheets}"/>
                    </WrapPanel>

                    <WrapPanel Margin="0,8,0,0">
                        <Button Content="Comparar" Width="110" Command="{Binding CompareCommand}"/>
                        <Button Content="Cancelar" Width="110" Margin="8,0,0,0" Command="{Binding CancelCommand}"/>
                    </WrapPanel>

                    <TextBlock Margin="0,8,0,0" Text="{Binding StatusText}"/>
                    <ProgressBar Height="16" Margin="0,4,0,0" Minimum="0" Maximum="100" Value="{Binding ProgressValue}"/>
                </StackPanel>

                <GroupBox Header="Resumen" DockPanel.Dock="Bottom">
                    <TreeView ItemsSource="{Binding SummaryItems}" SelectedItemChanged="TreeSummary_SelectedItemChanged">
                        <TreeView.Resources>
                            <HierarchicalDataTemplate DataType="{x:Type models:SummaryTreeItem}" ItemsSource="{Binding Children}">
                                <TextBlock Text="{Binding Header}"/>
                            </HierarchicalDataTemplate>
                        </TreeView.Resources>
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem">
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded}"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>
                </GroupBox>
            </DockPanel>

            <GroupBox Grid.Column="2" Header="Diferencias">
                <DockPanel>
                    <TextBox DockPanel.Dock="Top" Margin="6" Height="26"
                             Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGrid Margin="6" AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" SelectionMode="Single"
                              GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                              AlternatingRowBackground="#FFF5F5F5" AlternationCount="2"
                               RowHeaderWidth="0"
                               ItemsSource="{Binding DiffsView}">
                        <DataGrid.Resources>
                            <Style x:Key="WrapCell" TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="Padding" Value="2"/>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Sheet" Binding="{Binding Sheet}" Width="140" ElementStyle="{StaticResource WrapCell}"/>
                            <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="90" ElementStyle="{StaticResource WrapCell}"/>
                            <DataGridTextColumn Header="Kind" Binding="{Binding Kind}" Width="90" ElementStyle="{StaticResource WrapCell}"/>
                            <DataGridTextColumn Header="What" Binding="{Binding What}" Width="150" ElementStyle="{StaticResource WrapCell}"/>
                            <DataGridTextColumn Header="Before" Binding="{Binding Before}" Width="*" ElementStyle="{StaticResource WrapCell}"/>
                            <DataGridTextColumn Header="After" Binding="{Binding After}" Width="*" ElementStyle="{StaticResource WrapCell}"/>
                        </DataGrid.Columns>
                        <DataGrid.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock FontWeight="Bold" Background="#FFEFEFEF" Padding="4" Text="{Binding Name}"/>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                                <GroupStyle.Panel>
                                    <ItemsPanelTemplate>
                                        <DataGridRowsPresenter/>
                                    </ItemsPanelTemplate>
                                </GroupStyle.Panel>
                            </GroupStyle>
                        </DataGrid.GroupStyle>
                    </DataGrid>
                </DockPanel>
            </GroupBox>
        </Grid>

        <TextBlock Grid.Row="3" Margin="0,10,0,0" Foreground="Gray"
                   Text="MVP: compara hojas (add/remove/hidden) + celdas (valor/fórmula). OpenXML no recalcula fórmulas."/>
    </Grid>

</Window>
